const { Triangle, Circle, Square } = require('./lib/shapes');

// Jest tests for shapes
test('Triangle should...', () => {
    // ... (Triangle test)
});

test('Circle should...', () => {
    // ... (Circle test)
});

test('Square should...', () => {
    // ... (Square test)
});
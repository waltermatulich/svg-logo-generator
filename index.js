const { Triangle, Circle, Square } = require('./lib/shapes');

function createLogo() {
    // ... (use the classes from the lib folder)
}

function createShapeElement(shape, color) {
    // ... (use the classes from the lib folder)
}

module.exports = { createLogo };